<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <!--
      Exposes only explicit share intents. Its root covers /storage so a file
      in a removable volume can be shared without an internal cache copy.
    -->
    <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.CopperExternalFileProvider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/copper_external_paths" />
    </provider>
  </application>
</manifest>
